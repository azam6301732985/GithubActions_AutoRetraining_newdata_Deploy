name: Regression CI/CD

on:
  push:
    paths:
      - 'data/train_new.csv'
      - 'train_model.py'
      - '.github/workflows/**'

jobs:
  retrain-and-evaluate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.10

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run training script
        run: |
          mkdir -p model
          python train_model.py

      - name: Upload model artifact (pinned SHA)
        uses: actions/upload-artifact@9a66be8f5cf8f2aa38b4aab84b6e9fdf4d150a1c  # v3.1.2
        with:
          name: trained-model
          path: model/

